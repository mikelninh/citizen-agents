# 🧪 QA Playtester — 2026-08-30

> Verdict: **SHIP** (with notes). Reviewing `studio/engineer-2026-08-21` (D1 gate).
> Branch: `studio/qa-2026-08-30`. Repo: `bla-keks-world`.

## What the engineer shipped (D1 — the gate)
Bots stopped firing a dead invisible hitscan carbine and now fire the **same
dodgeable, visible soak `Projectiles` the player uses** (randomized splatter /
bubbler / confetti / petalbow per tactical bot, gated by a real `Charge`), and
the host now resolves bot-owned projectile hits. `weapon.js` had the dead
hitscan code (`WEAPON`, `Magazine`, `buildViewModel`, `traceShot`, `raySphere`)
deleted — only `applySpread` survives.

## Verification
- **Build — green.** `cd experiences/little-planet && npm install && npm run build`
  → 56 modules transformed, built ok. Only the pre-existing `>500 kB` chunk
  warning remains. Note: the **root** `vite build` does *not* include
  `experiences/little-planet` (its inputs are world/dreamspace/duel), so this
  little-planet build is the only one that actually exercises the change.
- **Server — boots clean.** `node experiences/little-planet/server/node.js`
  printed the listen banner, answered `GET / → 200` (serving the freshly built
  `dist/`), and was `LISTEN` on `:8787` with no exceptions, then killed.
- **Static contract review — all resolve.** `Charge.ready/.use/.step`,
  `statsFor(id)` shape (`speed/gravity/radius/color/soak/projectile/kind`,
  `slow` safely `?? 0`), `projectiles.spawn` key set, and `audio.splat(kind)`
  all exist and match what `botbrain.js` / `match-mode.js` pass.
- **Dead-code sweep — clean.** No live references to the deleted exports remain
  (only descriptive comment text in `weapon.js`).

## Issues
- **LOW** (polish, non-blocking): `botShoot` computes the projectile origin from
  `dir` before `dir.normalize()`; the offset is along a near-unit vector so the
  spawn point is imperceptibly off. No action needed to ship.

## What I could NOT test (honest)
- Real in-browser combat *feel* (readability, dodging, soak feedback) — CI can't
  playtest.
- Live multiplayer host/client soak resolution — verified by static contract
  only, not with a second real client.
- The full soak→pop **win condition** (D2) is deferred; this PR makes bot shots
  visible/dodgeable + host-resolved, not yet a complete win loop.
- Root repo `npm test` checks were intentionally skipped (they don't bundle
  little-planet).

## Verdict
**SHIP.** No blockers, no regressions, build + server both green, dead code
fully removed. Explore and Hide & Seek modes still compile (same bundle).
Recommended next session: D2 (soak as the single win condition + stamina-gated
saber) on top of this.
