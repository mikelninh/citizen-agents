# 🧪 QA Playtester — Verdict: SHIP WITH NOTES (2026-08-12)

**Repo:** `mikelninh/blakeks-world` (note: the brief's `bla-keks-world` **404s** — wrong repo name broke the prior director/engineer runs)
**Branch:** `studio/qa-2026-08-12` · **Head tested:** `18abede` (Align v0.32 Arena release note)
**Verdict:** ✅ **SHIP the baseline** — every automated gate is green. ⚠️ But the **studio pipeline is broken**, so the director's directives were never implemented.

## What I actually verified
- `npm test` (the canonical gate) — **PASS**:
  - `check-project`: 6 DOM anchors, 102 PNG assets, 4 foundations ✅
  - `check-duel-fairness`: **70/70** (determinism, 8-form mirror + pairwise, no hard counters) ✅
  - `check-arena-runtime`: **44/44** (cold boot, full match = no NaN, no console errors, rematch, hot-seat, all 8 Worldforms) ✅
  - `check:units`: **182 pass / 183 run / 0 fail** (1 skipped/ignored, not a failure) ✅
  - `vite build`: ✓ built in 21.41s (only the pre-existing 1.3 MB chunk-size warning) ✅
- **Server boot**: `node server/node.js` → `🍪 Bla Keks World server on :8787`, no throw, no crash ✅

## Issues found
- 🔴 **BLOCKER (pipeline):** No `studio/*` branches and no `agent-digests/`/`agent-logs/` exist in the repo. The director's and engineer's cron jobs used `mikelninh/bla-keks-world`, which does not exist (real name has **no hyphen**: `blakeks-world`). Their `gh repo clone` failed, so **none of the director's directives were ever implemented or PR'd** — there were no engineer changes for me to verify this run. Fix the repo name in all studio-agent prompts and re-run.
- 🟠 **HIGH (brief vs reality):** The injected Studio Director brief is partly based on a misread of the code. It says "`src/modes/weapon.js` is dead, bots hitscan-snipe at 130m, kill it." **False** — `weapon.js` is **live**: imported by `botbrain.js` (`tacticalBrain`→`botShoot`→`traceShot`) and by `match-mode.js` (`applySpread`). Per-weapon `Charge` **already exists** (`match-mode.js:898`). The *real* asymmetry is accurate but mis-framed: the **player lobs dodgeable projectile arcs** (`Projectiles` pool) while **bots fire hitscan** (`botShoot`). Don't delete `weapon.js` — that would delete bot combat. If addressing fairness, convert bot fire to projectiles or dial bot inaccuracy. The "Duel Triangle" (RMB guard/deflect), "shared Focus meter," and "Blade vs Petal classes" are **unimplemented aspirational directives**, not regressions.
- 🟡 **LOW (code smell):** `_pendingSwing` in `match-mode.js` (~976–988) is never reset on a whiffed saber swing — only inside the connecting-hit loop. A missed swing leaves a permanent per-frame `saber.hits()` call. Harmless (self-heals on next connecting swing, no double-charge), but worth a one-line reset.

## What I could NOT test (honest untested list)
- Live 2+ player networked match over WebSocket — server booted but no clients connected; only offline sim + headless arena harness exercised.
- Combat **feel** (saber timing, projectile dodgeability, bot readability) — automated QA cannot playtest feel; needs a human.
- Mobile/touch input and low-end-GPU performance.
- The 1 of 183 unit tests that didn't count as pass (skipped/ignored, not a failure).

## Bottom line
The game *code* is healthy and shippable. The thing that's broken is the **agent loop itself** — wrong repo name means the director and engineer never actually shipped anything. Fix that first; then QA will have real diffs to gate.
